<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Video Chat App</title>
    <link rel="stylesheet" href="style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script>
      const ROOM_ID = "<%= roomId %>";
    </script>
    <style>
      html, body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
        overflow: hidden;
        /* Dark blue gradient background */
        background: linear-gradient(135deg, #232526 0%, #0f2027 50%, #2c5364 100%);
      }
      .container {
        display: flex;
        height: 100vh;
        width: 100vw;
      }
      .sidebar {
        width: 0;
        min-width: 0;
        max-width: 0;
        overflow: hidden;
        transition: width 0.3s, min-width 0.3s, max-width 0.3s;
      }
      .sidebar.active {
        width: 220px;
        min-width: 180px;
        max-width: 220px;
        background: rgba(44,83,100,0.97);
        color: #fff;
        border-radius: 0 18px 18px 0;
        box-shadow: 2px 0 12px rgba(0,0,0,0.18);
        padding: 24px 16px;
        z-index: 1000;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
      }
      .header {
        flex: 0 0 auto;
        padding: 16px 32px;
        background: rgba(20, 30, 40, 0.85);
        display: flex;
        align-items: center;
        justify-content: space-between;
      }
      .logo {
        display: flex;
        align-items: center;
      }
      .header__back {
        margin-right: 16px;
        cursor: pointer;
        font-size: 1.5rem;
        color: #fff;
        transition: color 0.2s;
      }
      .header__back:hover {
        color: #00bcd4;
      }
      h3 {
        margin: 0;
        font-size: 1.6rem;
        font-weight: 600;
        letter-spacing: 1px;
      }
      .main {
        flex: 1;
        display: flex;
        flex-direction: row;
        height: calc(100vh - 48px); /* Subtract header height, adjust as needed */
        align-items: center; /* Vertically center both sections */
      }
      .main__left, .main__right {
        min-height: 0;
        min-width: 0;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        justify-content: center; /* Center content vertically */
        align-items: center;
        padding-top: 24px;
        padding-bottom: 24px;
        height: 100%; /* Match heights */
        padding-bottom: 48px;   /* More space below */
        margin-bottom: 24px;    /* Extra margin below */
      }
      .main__left {
        flex: 3;
        position: relative;
        padding-bottom: 160px;   /* More space below for options bar */
      }
      .main__right {
        flex: 1;
        background: rgba(44,83,100, 0.92);
        border-radius: 18px 0 0 18px;
        min-width: 260px;
        max-width: 340px;
      }
      .videos__group {
        flex: 1;
        min-height: 0;
        min-width: 0;
        overflow-y: auto;
        width: 100%;
        background: rgba(44,83,100,0.12);
        border-radius: 18px 18px 0 0;
        box-shadow: 0 4px 24px rgba(0,0,0,0.18);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 0;
      }
      #video-grid {
        width: 100%;
        height: 100%;
        display: flex;
        flex-wrap: wrap;
        gap: 16px;
        align-items: center;
        justify-content: center;
      }
      .options {
        position: absolute;
        bottom: 140px;           /* Lift options bar higher above the bottom */
        left: 50%;
        transform: translateX(-50%);
        width: auto;
        background: rgba(0,0,0,0.6); 
        border-radius: 12px;
        padding: 10px 20px;
        display: flex;
        gap: 16px;
        z-index: 10;
      }
      .options__left, .options__right {
        display: flex;
        gap: 18px;
      }
      .options__button {
        background: linear-gradient(135deg, #0f2027 0%, #2c5364 100%);
        border: none;
        border-radius: 50%;
        width: 54px;
        height: 54px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.6rem;
        color: #fff;
        cursor: pointer;
        box-shadow: 0 2px 8px rgba(0,0,0,0.18);
        transition: background 0.2s, transform 0.2s;
      }
      .options__button:hover {
        background: linear-gradient(135deg, #00bcd4 0%, #232526 100%);
        transform: scale(1.08);
      }
      .main__chat_window {
        flex: 1;
        overflow-y: auto; /* Enable vertical scrollbar */
        min-height: 0;
        margin-bottom: 8px;
      }
      .messages {
        font-size: 1rem;
      }
      .message {
        margin-bottom: 10px;
        padding: 8px 12px;
        background: rgba(44,83,100,0.18);
        border-radius: 10px;
        color: #e0eaff;
      }
      .timestamp {
        font-size: 0.85em;
        color: #bbb;
        margin-left: 8px;
      }
      .main__message_container {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      #chat_message {
        flex: 1;
        padding: 10px;
        border-radius: 8px;
        border: none;
        font-size: 1rem;
        background: rgba(44,83,100,0.22);
        color: #fff;
      }
      #chat_message::placeholder {
        color: #bbb;
      }
      /* Participants sidebar overlay */
      .user-list {
        position: fixed;
        top: 64px;
        right: 0;
        width: 260px;
        height: calc(100vh - 64px);
        background: rgba(44,83,100,0.97);
        color: #fff;
        border-radius: 18px 0 0 18px;
        box-shadow: -2px 0 12px rgba(0,0,0,0.18);
        padding: 24px 16px;
        z-index: 1000;
        transform: translateX(100%);
        transition: transform 0.3s;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
      }
      .user-list.active {
        transform: translateX(0);
      }
      .user-list h4 {
        margin: 0 0 12px 0;
        font-size: 1.1rem;
        font-weight: 600;
        letter-spacing: 1px;
      }
      #participants {
        list-style: none;
        padding: 0;
        margin: 0;
        width: 100%;
      }
      #participants li {
        margin-bottom: 8px;
        padding: 6px 10px;
        background: rgba(255,255,255,0.08);
        border-radius: 8px;
        font-size: 1rem;
      }
      @media (max-width: 900px) {
        .container {
          flex-direction: column;
        }
        .main__left, .main__right {
          width: 100%;
          min-width: unset;
          border-radius: 0;
        }
        .main__left {
          padding: 12px;
        }
        .main__right {
          padding: 12px;
        }
        .user-list {
          top: 56px;
          width: 100vw;
          border-radius: 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="header">
      <div class="logo">
        <div class="header__back">
          <i class="fas fa-angle-left"></i>
        </div>
        <h3>Talksy</h3>
      </div>
    </div>
    <div class="container">
      <div class="sidebar user-list" id="userListSidebar">
        <h4>Participants</h4>
        <ul id="participants"></ul>
      </div>
      <div class="main">
        <div class="main__left">
          <div class="videos__group">
            <div id="video-grid"></div>
          </div>
          <div class="options">
            <div class="options__left">
              <div id="participantsToggle" class="options__button" title="Show Participants">
                <i class="fas fa-users"></i>
              </div>
              <div id="stopVideo" class="options__button" title="Toggle Video">
                <i class="fas fa-video"></i>
              </div>
              <div id="muteButton" class="options__button" title="Toggle Audio">
                <i class="fas fa-microphone"></i>
              </div>
              <div id="shareScreen" class="options__button" title="Share Screen">
                <i class="fas fa-desktop"></i>
              </div>
              <div id="showChat" class="options__button" title="Show Chat">
                <i class="fas fa-comment"></i>
              </div>
              <div id="reactionButton" class="options__button" title="Send Reaction">
                <span>👍</span>
              </div>
              <div id="muteAllBtn" class="options__button" title="Mute All">
                <i class="fas fa-volume-mute"></i>
              </div>
            </div>
            <div class="options__right">
              <div id="inviteButton" class="options__button" title="Invite">
                <i class="fas fa-user-plus"></i>
              </div>
            </div>
          </div>
        </div>
        <div class="main__right">
          <div class="main__chat_window">
            <div class="messages"></div>
          </div>
          <div class="main__message_container">
            <input id="chat_message" type="text" autocomplete="off" placeholder="Type message here...">
            <div id="send" class="options__button" title="Send">
              <i class="fas fa-paper-plane"></i>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div id="notification" style="display:none;position:fixed;bottom:30px;right:30px;background:#232526;color:#fff;padding:12px 24px;border-radius:8px;box-shadow:0 2px 8px #000;z-index:2000;font-size:1rem;">
      New message received!
    </div>
    <script src="script.js"></script>
  </body>
</html>